# v2.7.13 — Fixes

---

## Bug 1: EL PAÍS critic reviews

**Archivo**: `app/src/main/java/com/dondeloexan/data/remote/filmaffinity/FilmaffinityScraper.kt`

### Cambio 1a — Fallback `ownText()` en publication (línea ~89)
```
// ANTES:
val publicationEl = row.selectFirst("td.author em a, td.author strong a, td.author em, td.author strong")
...
val publication = publicationEl?.text()?.trim() ?: ""

// DESPUÉS:
val publicationEl = row.selectFirst("td.author em a, td.author strong a, td.author em, td.author strong")
val publicationOwnText = row.selectFirst("td.author")?.ownText()?.trim()
...
val publication = publicationEl?.text()?.trim()
    ?: publicationOwnText?.takeIf { it.isNotBlank() }
    ?: ""
```

### Cambio 1b — Normalize quita NBSP (línea ~138)
```
// ANTES:
private fun String.normalize(): String =
    lowercase()
        .replace('á', 'a').replace('é', 'e').replace('í', 'i')
        .replace('ó', 'o').replace('ú', 'u').replace('ü', 'u')
        .replace('ñ', 'n')
        .trim()

// DESPUÉS:
private fun String.normalize(): String =
    lowercase()
        .replace('\u00a0', ' ')
        .replace('á', 'a').replace('é', 'e').replace('í', 'i')
        .replace('ó', 'o').replace('ú', 'u').replace('ü', 'u')
        .replace('ñ', 'n')
        .trim()
```

---

## Bug 2: Quitar badge de cine arriba-izquierda en cards

### Cambio 2a — LibraryItemCard.kt (líneas 246-456)

**Eliminar bloque `MovieOverlayBadge`** (líneas 246-254):
```
// ELIMINAR este bloque completo:
if (releaseDate != null && totalEpisodes == null) {
    MovieOverlayBadge(
        releaseDate = releaseDate,
        platforms = streamingPlatforms,
        modifier = Modifier
            .align(Alignment.TopStart)
            .padding(6.dp)
    )
}
```

**Eliminar todo el contenido de `MovieOverlayBadge`** (líneas 343-457): borrar la data class `CinemaInfo` y toda la función `MovieOverlayBadge`.

**En `PlatformBadgeRow`** (línea 525-571), en el `else` (cuando `logoUrl == null`), añadir caso especial para "Cine":
```
// ANTES (línea 546-557):
} else {
    Surface(
        shape = RoundedCornerShape(3.dp),
        color = Color.White.copy(alpha = 0.15f)
    ) {
        Text(
            text = platform.platformName.take(2),
            modifier = Modifier.padding(horizontal = 3.dp, vertical = 1.dp),
            style = UbuntuTypography.labelSmall,
            color = TextSecondary,
            fontSize = 8.sp
        )
    }
}

// DESPUÉS:
} else if (platform.platformName == "Cine") {
    Text("\uD83C\uDFAC", fontSize = 14.sp)
} else {
    Surface(
        shape = RoundedCornerShape(3.dp),
        color = Color.White.copy(alpha = 0.15f)
    ) {
        Text(
            text = platform.platformName.take(2),
            modifier = Modifier.padding(horizontal = 3.dp, vertical = 1.dp),
            style = UbuntuTypography.labelSmall,
            color = TextSecondary,
            fontSize = 8.sp
        )
    }
}
```

### Cambio 2b — SearchItemCard.kt (líneas 188-225)

**Eliminar bloque de cine** (líneas 188-225):
```
// ELIMINAR este bloque completo:
if (content.type.name == "MOVIE" && !content.releaseDate.isNullOrBlank()) {
    val nonCinemaPlatforms = content.streamingPlatforms.none { it.platformName != "Cine" }
    ...
}
```

**En `PlatformLogoRow`** (línea 415-428), en el `else`:
```
// ANTES (línea 416-427):
} else {
    Surface(
        shape = RoundedCornerShape(4.dp),
        color = Color.White.copy(alpha = 0.15f)
    ) {
        Text(
            text = platform.platformName.take(2),
            modifier = Modifier.padding(horizontal = 4.dp, vertical = 2.dp),
            style = UbuntuTypography.labelSmall,
            color = TextSecondary,
            fontSize = 9.sp
        )
    }
}

// DESPUÉS:
} else if (platform.platformName == "Cine") {
    Text("\uD83C\uDFAC", fontSize = 16.sp)
} else {
    Surface(
        shape = RoundedCornerShape(4.dp),
        color = Color.White.copy(alpha = 0.15f)
    ) {
        Text(
            text = platform.platformName.take(2),
            modifier = Modifier.padding(horizontal = 4.dp, vertical = 2.dp),
            style = UbuntuTypography.labelSmall,
            color = TextSecondary,
            fontSize = 9.sp
        )
    }
}
```

---

## Bug 3: Quitar texto "En cines" en detalle

**Archivo**: `app/src/main/java/com/dondeloexan/presentation/detail/MediaDetailScreen.kt`

### Cambio 3a — `appendCinemaPlatform()` (líneas 1185-1219)
```
// ANTES:
val cinemaLabel = try {
    val date = LocalDate.parse(releaseDate)
    val now = LocalDate.now()
    val daysSinceRelease = ChronoUnit.DAYS.between(date, now)
    val daysUntilRelease = ChronoUnit.DAYS.between(now, date)
    val cinemaEnd = date.plusDays(90)
    when {
        daysSinceRelease in 0..90 -> "En cines → Fin: ${cinemaEnd.format(DateTimeFormatter.ofPattern("dd/MM"))}"
        daysUntilRelease > 0 -> null
        else -> null
    }
} catch (e: Exception) { null }

val hasSubscription = platforms.any { it.availabilityType == AvailabilityType.SUBSCRIPTION }
return if (cinemaLabel != null && !hasSubscription) {
    val cinemaPlatform = StreamingAvailability(
        platformName = cinemaLabel,
        platformId = null,
        logoUrl = null,
        availabilityType = AvailabilityType.ADS
    )
    listOf(cinemaPlatform) + platforms
} else {
    platforms
}

// DESPUÉS:
try {
    val date = LocalDate.parse(releaseDate)
    val now = LocalDate.now()
    val daysSinceRelease = ChronoUnit.DAYS.between(date, now)
    val daysUntilRelease = ChronoUnit.DAYS.between(now, date)
    val hasSubscription = platforms.any { it.availabilityType == AvailabilityType.SUBSCRIPTION }
    val isInCinemas = daysSinceRelease in 0..90 && !hasSubscription
    val isFuture = daysUntilRelease > 0 && !hasSubscription

    if (isInCinemas || isFuture) {
        val dateLabel = if (isInCinemas) date.format(DateTimeFormatter.ofPattern("dd/MM/yyyy")) else date.format(DateTimeFormatter.ofPattern("dd/MM/yyyy"))
        val cinemaPlatform = StreamingAvailability(
            platformName = "Cine",
            platformId = null,
            logoUrl = null,
            availabilityType = AvailabilityType.SUBSCRIPTION
        )
        listOf(cinemaPlatform) + platforms
    } else {
        platforms
    }
} catch (e: Exception) {
    platforms
}
```

### Cambio 3b — `PlatformCard()` (líneas 1299-1350), cambiar label de cine
```
// ANTES (línea 1306, 1342-1348):
if (platform.platformName.contains("Cine", ignoreCase = true) || platform.platformName.contains("Estreno", ignoreCase = true)) {
    Box(...) {
        Text("\uD83C\uDFAC", fontSize = 20.sp)
    }
}
...
Text(
    platform.platformName,
    ...
    maxLines = 1
)

// DESPUÉS:
if (platform.platformName == "Cine") {
    Box(...) {
        Text("\uD83C\uDFAC", fontSize = 20.sp)
    }
}
...
Text(
    if (platform.platformName == "Cine") releaseDate else platform.platformName,
    color = if (platform.platformName == "Cine") EleganteRose else TextSecondary,
    fontSize = 9.sp,
    maxLines = 1
)

// Nota: PlatformCard necesita recibir releaseDate como parámetro
```

### Cambio 3c — `StreamingSection()` (líneas 1221-1297), eliminar sección "Cine" separada
```
// ANTES (línea 1224):
val cinemaPlatforms = platforms.filter { it.platformName.contains("Cine", ignoreCase = true) || it.platformName.contains("Estreno", ignoreCase = true) }

// DESPUÉS (eliminar esa línea, ya no se separa cine del resto):
// Las plataformas de cine se integran con el resto (Subscription)
```

### Cambio 3d — `FichaTab()` pasar releaseDate a StreamingSection
```
// ANTES (línea 648):
item { StreamingSection(displayPlatforms, futureReleaseLabel, futurePlatformInfo) }

// DESPUÉS:
item { StreamingSection(displayPlatforms, futureReleaseLabel, futurePlatformInfo, content.releaseDate) }
```

---

## Versión

**Archivo**: `app/build.gradle.kts`
```
versionCode = 95
versionName = "2.7.13"
```

---

## Caché

**Archivo**: `app/src/main/java/com/dondeloexan/data/repository/DiscoverRepositoryImpl.kt`
Mantener `criticReviewDao.deleteAll()` temporalmente para forzar refetch con el nuevo parser. Se eliminará en la siguiente versión cuando se confirme que EL PAÍS aparece correctamente.
